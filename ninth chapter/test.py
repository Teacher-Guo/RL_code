import numpy as np
import tensorflow as tf
# rs = [1,2,3,4]
# reward_sum = 0
# discouted_sum_reward = np.zeros_like(rs)
# for t in reversed(range(0, len(rs))):
#     print(t)
#     reward_sum =  rs[t]
#     discouted_sum_reward[t] = reward_sum
# print(discouted_sum_reward)
class